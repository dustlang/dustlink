// DustLink - Object File Integration Module
// Handles linking DPL object files with external linkers

forge LinkerLink {
    
    const LINKER_NONE: UInt32 = 0;
    const LINKER_LD: UInt32 = 1;
    const LINKER_LD64: UInt32 = 2;
    const LINKER_CLANG: UInt32 = 3;
    const LINKER_GCC: UInt32 = 4;
    
    const FORMAT_ELF64: UInt32 = 1;
    const FORMAT_FLAT: UInt32 = 2;
    const FORMAT_MBR: UInt32 = 3;
    
    const TARGET_X86_64_NONE: UInt32 = 0;
    const TARGET_X86_64_LINUX: UInt32 = 1;
    const TARGET_X86_64_WINDOWS: UInt32 = 2;
    const TARGET_X86_64_MACOS: UInt32 = 3;
    
    proc K::add_object_file(path: UInt64) -> UInt32 {
        return 0;
    }
    
    proc K::add_library(path: UInt64) -> UInt32 {
        return 0;
    }
    
    proc K::add_search_path(path: UInt64) -> UInt32 {
        return 0;
    }
    
    proc K::set_linker(linker: UInt32) -> UInt32 {
        return 0;
    }
    
    proc K::set_target(target: UInt32) -> UInt32 {
        return 0;
    }
    
    proc K::set_output(output: UInt64) -> UInt32 {
        return 0;
    }
    
    proc K::set_entry(entry: UInt64) -> UInt32 {
        return 0;
    }
    
    proc K::set_base_address(base: UInt64) -> UInt32 {
        return 0;
    }
    
    proc K::link() -> UInt32 {
        return 0;
    }
    
    proc K::link_with_script(script: UInt64) -> UInt32 {
        return 0;
    }
    
    proc K::create_flat_binary(output: UInt64, entry: UInt64) -> UInt32 {
        return 0;
    }
    
    proc K::create_mbr_image(output: UInt64, boot_sector: UInt64, kernel: UInt64) -> UInt32 {
        return 0;
    }
    
    proc K::get_linker_path() -> UInt64 {
        return 0;
    }
    
    proc K::find_linker() -> UInt32 {
        return 0;
    }
    
    proc Q::add_object_file(path: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Q::add_library(path: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Q::add_search_path(path: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Q::set_linker(linker: UInt32) -> UInt32 {
        return 100;
    }
    
    proc Q::set_target(target: UInt32) -> UInt32 {
        return 100;
    }
    
    proc Q::set_output(output: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Q::set_entry(entry: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Q::set_base_address(base: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Q::link() -> UInt32 {
        return 100;
    }
    
    proc Q::link_with_script(script: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Q::create_flat_binary(output: UInt64, entry: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Q::create_mbr_image(output: UInt64, boot_sector: UInt64, kernel: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Q::get_linker_path() -> UInt64 {
        return 0;
    }
    
    proc Q::find_linker() -> UInt32 {
        return 100;
    }
    
    proc Phi::add_object_file(path: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Phi::add_library(path: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Phi::add_search_path(path: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Phi::set_linker(linker: UInt32) -> UInt32 {
        return 100;
    }
    
    proc Phi::set_target(target: UInt32) -> UInt32 {
        return 100;
    }
    
    proc Phi::set_output(output: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Phi::set_entry(entry: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Phi::set_base_address(base: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Phi::link() -> UInt32 {
        return 100;
    }
    
    proc Phi::link_with_script(script: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Phi::create_flat_binary(output: UInt64, entry: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Phi::create_mbr_image(output: UInt64, boot_sector: UInt64, kernel: UInt64) -> UInt32 {
        return 100;
    }
    
    proc Phi::get_linker_path() -> UInt64 {
        return 0;
    }
    
    proc Phi::find_linker() -> UInt32 {
        return 100;
    }
}
